/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['i.pinimg.com', 'your-image-domain.com'], // Add any additional domains as needed
  },
};

export default nextConfig;
